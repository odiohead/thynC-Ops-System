# AI 어시스턴트 고도화 설계안 — ① 토큰 최적화 ② 위키 카테고리 스코프

> 작성 2026-07-25 / 상태: **Part A 구현 완료 (DEV 검증까지) · Part B 폐기 · PROD 미반영**
> 결과 요약은 문서 하단 §8 참조. 작업 이력은 `DEV_HISTORY.md` 2026-07-25 항목.
> 관련 문서: `function_ai_assistant.html`(v2 원설계), `README.md` § AI 어시스턴트 v2
> 대상 코드: `lib/ai/agent.ts`, `lib/ai/tools.ts`, `app/api/ai-assistant/*`, `lib/wiki/aiExclusion.ts`

---

## 0. 요약

| 구분 | 현재 | 목표 |
|---|---|---|
| 답변 1건당 총 프롬프트 | 약 27,500 토큰 | 약 27,000 토큰(유사) — **단, 유료 비중을 캐시로 이동** |
| 캐시 적중률 | **51%** (시스템+도구정의만) | **85% 이상** |
| 답변 1건당 예상 비용 | 약 **$0.105** (약 150원) | 약 **$0.060~0.070** (약 90~100원) |
| 위키 검색 대상 | 전체 109페이지 (제외 지정만 가능) | 관리자가 지정한 **허용 카테고리** 하위로 한정 |

**비용 절감 예상 30~40%.** 가장 큰 단일 원인은 "에이전트 루프가 도구 결과를 누적하면서 매 반복마다 대화 전체를 정가로 재처리"하는 구조이며, 이는 코드 1개 파일 수정으로 해결됩니다.

---

## 1. 현황 실측 (DEV `ai_usage_logs` 33건 기준)

```
평균 uncached input : 11,417 토큰   ← 정가 청구
평균 cache read     : 14,069 토큰   ← 0.1배 청구
평균 cache write    :  1,972 토큰   ← 1.25배 청구
평균 output         :  1,139 토큰   ← 사고(thinking) 토큰 포함
최대 uncached input : 53,760 토큰
```

### 1-1. 비용 구성 (opus-4-8 $5 / $25 per MTok 기준)

| 항목 | 토큰 | 비용 | 비중 |
|---|---:|---:|---:|
| **uncached input** | 11,417 | **$0.0571** | **54.4%** |
| output (사고 포함) | 1,139 | $0.0285 | 27.1% |
| cache write | 1,972 | $0.0123 | 11.8% |
| cache read | 14,069 | $0.0070 | 6.7% |
| **합계** | | **$0.1049** | 100% |

### 1-2. 도구 호출 분포 (assistant 메시지 30건)

| 도구 호출 수 | 0 | 1 | 2 | 3 | 4 | 5 | 7 | 8 | 9 | 11 | 13 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 건수 | 1 | 10 | 4 | 3 | 2 | 1 | 1 | 4 | 2 | 1 | 1 |

**8회 이상이 8건(27%)** — 루프 반복이 많은 답변이 비용을 지배합니다.

---

## 2. 문제 진단

### 문제 A-1 ★★★ — 에이전트 루프 messages에 캐시 브레이크포인트 없음

`lib/ai/agent.ts:65-68`

```typescript
const system: Anthropic.TextBlockParam[] = [
  { type: 'text', text: SYSTEM_PROMPT, cache_control: { type: 'ephemeral' } },  // ← 유일한 브레이크포인트
  { type: 'text', text: `<context>...` },
]
```

렌더 순서는 `tools → system → messages`입니다. 브레이크포인트가 시스템 프롬프트에만 있으므로 **도구 정의 + 시스템 프롬프트(약 1,972토큰)까지만 캐시**되고, `messages` 배열은 통째로 캐시 대상 밖입니다.

`agent.ts:79-142`의 루프는 반복마다 `messages.push(assistant tool_use)` → `messages.push(tool_result)`로 배열을 키우면서 최대 8회 재호출합니다. 즉 **N번째 반복에서 1~N-1번째 도구 결과 전체를 다시 정가로 처리**합니다.

**실측으로 확인됨**: cache write 평균 1,972 = prefix 크기, cache read 평균 14,069 ÷ 1,972 ≈ 7.1회 — 캐시가 prefix만 반복해서 읽고 있을 뿐 messages는 전혀 커버하지 못합니다.

### 문제 A-2 ★★ — 도구 응답 페이로드가 고정 최대치

- `list_projects` / `list_maintenances` / `list_site_visits` / `list_install_plans` / `list_etc_tasks` 5종이 전부 `take: 30` 고정, 필드 축약 없이 전량 반환.
  실측(DEV): 유지보수 30건 직렬화 약 5.3KB. **PROD는 증상·조치요약이 길어 2~4배 예상.**
- `read_wiki_page`는 8,000자 고정 절단. **8,000자 초과 페이지가 11건(최대 68,772자)** 존재 → 뒷부분에 답이 있으면 영영 못 읽고 재검색·재호출을 유발.
- "몇 건인가"만 물어도 30건 상세가 전부 컨텍스트에 들어옵니다.

### 문제 A-3 ★★ — search_wiki 검색 품질이 낮아 불필요한 read를 유발

`lib/ai/tools.ts:654-684`

1. **정렬이 관련도가 아니라 `updatedAt desc`** — 매칭된 페이지 중 "가장 최근 수정된" 10건을 반환. 정답 페이지가 오래된 문서면 목록에서 밀려납니다.
2. **질의 전체 문자열을 단일 `contains`로 검색** — 한국어 조사 때문에 리콜이 급락합니다. `"게이트웨이 오프라인"`은 본문의 `"게이트웨이가 오프라인"`에 매칭되지 않습니다.
3. 검색이 빗나가면 → 모델이 엉뚱한 페이지를 `read_wiki_page`(최대 8,000자)로 읽음 → 다시 검색 → 반복. **A-1의 누적 비용을 증폭**시킵니다.

`pg_trgm` GIN 인덱스는 이미 있으므로(README §WikiPage) 인프라 추가 없이 개선 가능합니다.

### 문제 A-4 ★ — effort 미지정(기본 high)

`agent.ts:80-87`은 `thinking: { type: 'adaptive' }`만 지정하고 `output_config.effort`가 없습니다. 기본값은 **high**입니다. "이번달 유지보수 몇 건?" 같은 단순 조회에도 최대 사고량이 걸리며, 사고 토큰은 output 단가($25/MTok)로 청구됩니다. output이 전체 비용의 27%입니다.

### 문제 A-5 — 히스토리 선형 증가

`chat/route.ts:10` `HISTORY_LIMIT = 30`. 최근 30개 메시지 전문을 매 턴 재전송합니다. **단, A-1을 고치면 대부분 캐시 히트(0.1배)로 흡수**되므로 후순위입니다.

### 문제 B — 위키 검색 범위를 "허용 목록"으로 지정할 수 없음

현재는 `ai_excluded` **제외 방식만** 존재합니다(`lib/wiki/aiExclusion.ts`). 새 카테고리가 추가되면 자동으로 검색 대상에 포함되고, 관리자가 매번 제외를 눌러야 합니다.

현재 위키 최상위 구조 (총 109페이지, 평균 2,557자):

| 최상위 카테고리 | 하위 | 성격 |
|---|---:|---|
| thynC운영팀 wiki | 3 | 현행 사내 문서 |
| thync_1.3.0 | 12 | 제품 산출물(HTML 12종) |
| 프로젝트 이슈노트 | 23 | 시스템 카테고리 |
| 병원 노트 | 3 | 시스템 카테고리 |
| 운영시스템 개선요청사항 | 0 | |
| **Old_thynC 운영팀 사내 위키** | **47** | **구 위키 — 노이즈 최대 후보** |

구 위키 47페이지를 범위에서 빼면 검색 대상이 **109 → 62 (43% 감소)** 하며, 이는 A-3의 오검색·불필요 read를 직접적으로 줄입니다. **② 요구사항과 ① 토큰 최적화는 서로를 강화합니다.**

---

## 3. 개선 설계

### Part A — 토큰 최적화

#### A-1. 롤링 캐시 브레이크포인트 (최우선)

에이전트 루프의 각 반복 직전, `messages`의 **마지막 콘텐츠 블록**에 `cache_control: { type: 'ephemeral' }`을 부여합니다. 다음 반복은 직전 반복이 쓴 캐시를 prefix로 읽습니다.

제약 2가지를 반드시 지켜야 합니다.

- **브레이크포인트는 요청당 최대 4개** → 롤링 관리(최신 1~2개만 유지, 이전 것 제거)
- **lookback 윈도우 20블록** → 한 턴에 20블록 이상 쌓이면 직전 캐시를 못 찾고 조용히 miss. 도구 8회면 블록이 16개 이상 누적되므로 **직전·최신 2개 유지** 방식으로 설계합니다.

```
반복 i:  [tools+system 캐시] + [messages 0..i-1 캐시] + [delta_i 신규]
                0.1배              0.1배                 1.25배
```

효과 추정: uncached input 비용이 **55~65% 감소**. 총 비용 기준 **30~35% 절감**.

> TTL은 기본 5분을 유지합니다. 대화형이라 턴 간격이 짧고, 1시간 TTL은 쓰기 단가가 2배(6.25→10)라 손익이 불리합니다. 다만 `ai_cache_ttl` 설정값으로 열어두어 나중에 실측 후 조정 가능하게 합니다.

#### A-2. 도구 응답 페이로드 축소

| 변경 | 내용 |
|---|---|
| `limit` 파라미터 | `list_*` 5종에 추가. **기본 10, 최대 30**. (현재 고정 30) |
| `detail` 파라미터 | `'summary'`(기본) / `'full'`. summary는 코드·병원·상태·날짜 등 핵심 필드만. 증상·조치요약·최근기록은 `full`에서만 |
| `read_wiki_page` | `offset`(기본 0)·`maxChars`(기본 6000, 최대 12000) 추가. 절단 시 `nextOffset`을 응답에 포함해 이어 읽기 가능하게 |
| 집계 우선 유도 | 시스템 프롬프트에 "건수·집계만 필요하면 `aggregate_stats`를 쓰고 `list_*`를 호출하지 말 것" 문구 강화 |

> `count`·`total`은 항상 반환하므로 "몇 건인가" 질문은 `limit=1`로도 정확히 답할 수 있습니다.

#### A-3. search_wiki 검색 품질 개선

1. **질의 토큰 분리 AND 매칭** — 공백 기준 분리 후 각 토큰을 `ILIKE '%토큰%'`으로 AND 결합. 한국어 조사 문제 해소.
2. **관련도 정렬** — `pg_trgm`의 `similarity()`로 점수화, **제목 매칭에 가중치**. `updatedAt`은 동점 시 tiebreaker로만 사용.
3. **응답에 카테고리 경로 병기** — `"thync_1.3.0 > 알람정책"` 형태. 모델이 어느 페이지를 읽을지 정확히 판단 → read 호출 감소.
4. 스니펫은 현행 유지(첫 매칭 주변 180자).

#### A-4. effort 명시

`output_config: { effort: ... }`를 지정합니다. AppSetting `ai_effort`로 관리자가 조절 가능하게 하여, 품질 회귀 시 즉시 되돌릴 수 있게 합니다.

**제안 기본값: `medium`** — 다만 이 값은 §5 결정 필요 항목이며, 회귀 질문셋 검증 후 확정합니다.

#### A-5. (후순위) 히스토리 관리

A-1 적용 후 실측하여 필요 시에만 착수. 현 시점 조치 없음.

---

### ~~Part B — 위키 카테고리 스코프~~ (폐기 — 2026-07-25)

> **개발하지 않음.** 요구사항은 "특정 카테고리만 제외"였고, 이는 기존 `ai_excluded` 기능으로 이미 충족된다:
> `wiki.wiki_pages.ai_excluded` 컬럼 + `lib/wiki/aiExclusion.ts` 재귀 CTE(조회 시점 계산 → 하위 자동 cascade,
> 페이지 이동에도 자동 반응) + `PATCH /api/wiki/pages/[id]/ai-exclude`(ADMIN) + 위키 페이지 메뉴 토글이
> 이미 있고, `search_wiki`·`read_wiki_page`·`read_hospital_note` **3개 도구 전부**에 적용되어 있다.
> 아래 허용 목록(allowlist) 설계는 요구사항을 반대로 뒤집은 과설계였으므로 폐기한다.
>
> 참고로 남는 공백 2가지(미착수, 필요 시 별도 논의): ① 현재 제외 지정 0건 ② 신규 최상위 카테고리는 기본
> 포함(`false`)이라 "다 위키화" 시 토글 누락 위험. 기존 카테고리 하위는 cascade로 자동 보호됨.

<details>
<summary>폐기된 원 설계 (기록용)</summary>

#### B-1. 허용 범위 저장 — AppSetting `ai_wiki_scope_page_ids`

```json
{ "ai_wiki_scope_page_ids": ["74c28499-...", "1c357b24-..."] }
```

- **빈 배열 = 전체 허용** → 현행 동작과 완전 하위호환. 관리자가 명시적으로 좁힐 때만 적용됩니다.
- 판정은 **저장 시점이 아니라 조회 시점에 재귀 CTE로 계산** — 기존 `aiExclusion.ts` 패턴을 그대로 따릅니다. 페이지를 옮기거나 하위를 추가해도 자동 반응합니다.
- **제외(`ai_excluded`)가 허용보다 우선.** 허용 범위 안이어도 제외 서브트리는 검색 대상이 아닙니다.
- 삭제된 페이지 ID가 설정에 남아도 조회 시 조인으로 자동 무시됩니다(stale 정리 불필요).

신규 파일 `lib/wiki/aiScope.ts`에 `getAiSearchablePageIds()`를 두고, 허용·제외를 **단일 쿼리**로 계산합니다(왕복 1회).

**대안 비교**

| 안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| ① AppSetting JSON | DDL 없음, 마이그레이션 없음, 롤백 즉시 | 페이지 목록 UI에서 별도 조회 필요 | **채택** |
| ② `wiki_pages.ai_scope` 컬럼 | `ai_excluded`와 대칭 | wiki 스키마 DDL 필요, 지정 대상이 루트 몇 개뿐인데 과설계 | 미채택 |
| ③ 별도 `wiki.ai_scope_pages` 테이블 | 정규화 | 위 대비 이점 없음 | 미채택 |

#### B-2. 도구 레벨 카테고리 지정 — `search_wiki`에 `category` 파라미터

허용 범위 **안에서** 에이전트가 스스로 더 좁힙니다. 예: 제품 사양 질문 → `category: "thync_1.3.0"`.

- 선택 가능한 카테고리 목록은 시스템 프롬프트가 아니라 **`<context>` 블록(캐시 브레이크포인트 뒤)** 에 주입합니다. → 카테고리가 바뀌어도 **프롬프트 캐시가 무효화되지 않습니다.** (`agent.ts:67`의 기존 컨텍스트 블록에 추가)
- 미지정 시 허용 범위 전체 검색(현행 동작).

#### B-3. 적용 대상 도구

| 도구 | 범위 규칙 적용 |
|---|---|
| `search_wiki` | ✅ 적용 |
| `read_wiki_page` | ✅ 적용 (id 직접 접근도 차단 — 현행 `isPageAiExcluded`와 동일 패턴) |
| `read_hospital_note` | ⚠️ **결정 필요** (§5-4) — 병원 노트는 전용 경로이므로 범위 밖이어도 허용할지 |

#### B-4. 관리 화면

**신규 `/settings/ai-assistant` (ADMIN 이상)** — AI 관련 설정을 한 곳에 모읍니다.

- 위키 검색 범위 (트리에서 카테고리 다중 선택)
- effort 설정 (`ai_effort`)
- 캐시 TTL (`ai_cache_ttl`) — 실측용
- 현재 검색 대상 페이지 수 미리보기 (예: "109개 중 62개")

> ⚠️ **CLAUDE.md 규칙 7 준수**: 설정 화면이 위키 트리를 표시해야 하지만 `app/settings/*`는 메인 모듈이므로 위키 코드 직접 import가 금지됩니다.
> → **`fetch('/api/wiki/tree')` HTTP 호출로 해결** (허용 방향, 쿠키 인증 자동 전달). 승인 예외 추가 불필요.

</details>

---

## 4. 단계 구성 및 검증

| Phase | 내용 | 영향 파일 | 기대 효과 |
|---|---|---|---|
| **P1** | A-1 롤링 캐시 브레이크포인트 | `lib/ai/agent.ts` | **비용 30~35%↓** |
| **P2** | A-2 페이로드 축소 + A-3 검색 품질 | `lib/ai/tools.ts` | 비용 추가 10%↓ + 정답률↑ |
| **P3** | B-1~B-4 위키 스코프 | `lib/wiki/aiScope.ts`(신규), `lib/ai/tools.ts`, `app/api/settings/ai-assistant/`, `app/settings/ai-assistant/` | 검색 정확도↑, 오검색 read 감소 |
| **P4** | A-4 effort 튜닝 + 계측 리포트 | `lib/ai/agent.ts`, 설정 화면 | output 비용 20~30%↓ |

### 검증 방법

1. **회귀 질문셋 12~15개 고정** — CS 응대 / 병원 현황 / 기간 집계 / 제품 사양 4축을 커버. 착수 전 사용자와 함께 확정.
2. 각 Phase 전후로 동일 질문셋 실행 → `ai_usage_logs` 비교. **기존 `/settings/ai-usage` 화면을 그대로 활용**(신규 계측 코드 불필요).
3. 판정 기준: **정답성 회귀 0건**이면서 토큰 감소. 정답성이 하나라도 깨지면 해당 Phase 롤백.
4. 캐시 검증: `cache_read_tokens / (input+cache_read+cache_write)` 비율이 **85% 이상**인지 확인. 0에 가까우면 브레이크포인트가 무효화된 것.

---

## 5. 결정 사항 (2026-07-25 확정)

| # | 항목 | 결정 |
|---|---|---|
| **1** | 채팅 본체 모델 | **`claude-opus-5`로 전환** (단가 동일 $5/$25). `max_tokens` 4096→16000 동반 상향 — opus-5는 사고가 기본 on이고 `max_tokens`가 사고+응답을 함께 덮으므로 |
| **2** | 상담정리(`summarize`) 모델 | **`claude-sonnet-5`로 하향** ($3/$15) |
| **3** | effort 기본값 | **`medium`** + `/settings/ai-assistant`에서 조절 |
| **4** | `read_hospital_note`에 위키 제외 규칙 적용 | **적용(포함)** — 확인 결과 [tools.ts:711](lib/ai/tools.ts#L711)에서 이미 `isPageAiExcluded`를 거치고 있어 코드 변경 없이 충족 |
| **5** | 설정 화면 위치 | **신규 `/settings/ai-assistant`** |

> 설계안의 `ai_effort`·`ai_cache_ttl` 분리 키 대신 **`ai_assistant_settings` JSON 단일 키**로 구현 (원자적 저장, 행 1개).
> `ai_usage_pricing` 선례와 동일한 패턴.

---

## 6. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 캐시 브레이크포인트 4개 제한·20블록 lookback 초과로 조용한 miss | 롤링 2개 유지 + 검증 4번(캐시 적중률 85% 확인)에서 즉시 감지 |
| effort 하향으로 복잡한 질문 품질 회귀 | AppSetting으로 즉시 원복. 회귀 질문셋 통과 전까지 기본값 변경 안 함 |
| 위키 범위를 좁혀 "예전엔 답하던 질문"을 못 답함 | 기본값 = 빈 배열(전체). 관리자가 명시적으로 좁힐 때만 적용 |
| `limit` 기본 10으로 낮춰 정보 누락 | `count`/`total`은 항상 반환, 부족 시 모델이 `limit` 올려 재호출 가능하도록 도구 설명에 명시 |
| 도구 스키마 변경으로 프롬프트 캐시 1회 무효화 | 배포 직후 1회만 발생, 무시 가능 |

---

## 7. 구현 결과 (2026-07-25, DEV)

### 7-1. 회귀 질문셋 5건 — 전부 정답, 회귀 0건

`npx tsx scripts/ai-agent-smoke.mts` (채팅 API를 거치지 않아 `ai_usage_logs`에 기록되지 않음 → 집계 오염 없음)

| 질문 | 도구 | uncached input | cacheRead | cacheWrite | output | 캐시적중 | 비용 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 제주한라병원 현황 | 2 | 6 | 12,103 | 6,369 | 378 | 65.5% | $0.0553 |
| 이번달 유지보수 건수 | 1 | 4 | 11,860 | 620 | 297 | 95.0% | $0.0173 |
| 진행중 구축 프로젝트 | 1 | 4 | 11,862 | 952 | 697 | 92.5% | $0.0293 |
| 알람 정책 기준 (위키) | 3 | 6 | 23,245 | 12,870 | 1,663 | 64.4% | $0.1337 |
| 병원 업무 전체 정리 | **5** | 6 | 18,044 | 908 | 550 | **95.2%** | $0.0285 |

**건당 평균 $0.0589** (기준선 $0.1049 대비 **44%↓**), 캐시 적중률 51% → 74~95%.

> 기준선 33건과 질문 구성이 달라 절감률은 참고치입니다. 구조적 증거는 **uncached input 11,417 → 4~6 토큰** —
> 루프가 더 이상 messages를 정가로 재처리하지 않습니다. 비용을 지배하던 다중 도구 호출(5회)에서 적중률 95.2%.

### 7-2. 동일 질문 A/B (P1 격리 측정)

캐시 로직만 일시 해제 후 즉시 복원(잔여 0건·`tsc` 0오류 확인). P2 페이로드 축소는 양쪽 모두 적용된 상태.

| | uncached input | cacheRead | cacheWrite | 비용 |
|---|---:|---:|---:|---:|
| 롤링 캐시 **on** | **6** | 18,044 | 908 | $0.0285 |
| 롤링 캐시 **off** | 1,722 | 17,619 | 0 | $0.0370 |

off 쪽 cacheRead 17,619 ÷ 6회 ≈ 2,936 = prefix 크기 → **캐시가 prefix만 반복 조회**하던 진단이 그대로 재현됨.

### 7-3. 검색 리콜

`"게이트웨이 설치"` 기준: 구 방식(전체 문자열 단일 매칭) **4건** → 신 방식(토큰 AND 매칭) **26건**.
관련도 정렬로 제목 적중 페이지가 상위에 오며, 위키 질문에서 정답 문서(`thync_1.3.0 > 08. 알람·부정맥 임상 정책서`)를 1회 검색으로 특정.

---

## 8. PROD 반영 체크리스트

DDL·마이그레이션·신규 패키지 **없음**. 스키마 변경이 없으므로 롤백은 코드 되돌리기만으로 충분합니다.

1. `git push` → PROD `git pull origin main`
2. **`psql -U thync -d thync_ops -f scripts/seed-ai-assistant-nav.sql`** (nav 메뉴 1행, 재실행 안전) — *사용자 명시 허락 후 실행*
3. `NODE_OPTIONS="--max-old-space-size=4096" npm run build && pm2 restart thync-prod`
4. 검증: `/settings/ai-assistant` 접근, 질문 1건 후 `/settings/ai-usage`에서 캐시 적중률 확인
5. 참고: 모델 전환으로 **기존 프롬프트 캐시는 1회 무효화**됩니다(첫 질문만 cacheWrite 발생, 정상)

> `/settings/ai-usage`의 단가(`ai_usage_pricing`) 기본값은 $5/$25/$0.5/$6.25로 opus-5와 동일하여 수정 불필요.
> 다만 상담 정리는 sonnet-5로 내려갔으나 원장(`ai_usage_logs`)에는 채팅만 기록되므로 집계에 영향 없습니다.
