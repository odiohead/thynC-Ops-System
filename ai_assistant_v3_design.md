# AI 어시스턴트 v3 설계안 — 출시 전 지식 아키텍처 재정비

> 작성 2026-07-25 / 상태: **V1·V2·V3 구현 완료 (DEV 검증까지) · W3 보류 · PROD 미반영**
> 구현 결과는 §10, 보류·미채택 항목은 §11 참조.
> 선행 문서: `function_ai_assistant.html`(v2 원설계), `ai_assistant_optimization_design.md`(토큰 최적화, 배포 완료)
> 전제: 어시스턴트는 아직 **공식 오픈 전**. 현재 사용 이력 33건은 내부 시험 흔적이므로 사용 패턴 데이터로 쓰지 않는다.

---

## 0. 설계 원리

사용자가 정의한 정보 소스 2축을 **서로 다른 검색 전략으로 다룬다.** 현재 v2는 두 축을 거의 같은 방식(단순 조회 도구 + 문서 단위 ILIKE)으로 다루고 있고, 이것이 구조적 한계의 근원이다.

| | **축 1 — 고정형 정보(위키)** | **축 2 — 운영 정보(운영관리 DB)** |
|---|---|---|
| 내용 | 버전별 형상·기능정의·매뉴얼·API 규격 | 유지보수 이력·영업 이력·상담 이력·답사 노트 |
| 단위 | 문서 (수천~수만 자) | 레코드 (평균 81~559자) |
| 내부 구조 | 헤딩 계층 (h1~h4) | 필드 스키마 (병원·유형·상태·날짜) |
| "정답"의 성격 | **하나** (단, 제품 버전별로 다름) | **여럿** (유사 사례 N건) |
| 갱신 빈도 | 낮음 (릴리스 단위) | 높음 (매일) |
| **필요 전략** | **청킹 + 버전 스코프** | **자유 텍스트 검색 신설 + 유사도 랭킹** |
| **현재 상태** | 문서 통짜 검색 + 6,000자 선형 읽기 | **자유 텍스트 검색 수단이 아예 없음** |

핵심 진단 두 줄:

- **축 1**은 검색은 되지만 **정밀도가 없다.** 68,772자 문서에서 200자를 찾으려고 6,000자씩 12번 읽는다.
- **축 2**는 **검색 자체가 불가능하다.** 구조화 필드(상태·기간·병원)로만 필터할 수 있고, 실제 노하우가 담긴 조치 내용·증상·답사 노트는 어떤 도구로도 찾을 수 없다.

---

## 1. 현황 실측

### 1-1. 축 1 — 위키 고정형 정보

`thync_1.3.0` 산출물 12종, 총 **216,609자**:

| 문서 | 형식 | 길이 |
|---|---|---:|
| 05. API 규격서 | html | **68,772** |
| 06. 데이터베이스 설계서 | html | **35,141** |
| 03. 기능정의서 — 서비스 콘솔 | html | 21,250 |
| 04. 기능정의서 — 관리자 콘솔 | html | 17,801 |
| 11. 용어·코드 정의서 | html | 15,133 |
| 07. 외부 연동 정의서 | html | 12,365 |
| 08. 알람·부정맥 임상 정책서 | html | 9,333 |
| 09. 설치·배포 가이드 | html | 9,156 |
| 10. 환경설정 정의서 | html | 8,429 |
| 02. 소프트웨어 아키텍처 정의서 | html | 9,012 |
| 01. 시스템 개요서 | html | 8,002 |
| 00. 산출물 개요 · 문서 인덱스 | html | 2,215 |

- **상위 3개 문서가 전체의 58%.** 크기 편차가 31배(2,215 ~ 68,772)
- 위키 전체 109페이지, 8,000자 초과 11건
- **다행인 점: 원본 구조가 살아 있다.** HTML 문서는 `content_html`에 헤딩 태그가, BlockNote 페이지는 `content_json`에 heading 블록이 보존돼 있다. 평탄화된 것은 검색용 `plain_text`뿐이므로 **청킹 재료가 이미 확보된 상태**다

### 1-2. 축 2 — 운영 정보 자유 텍스트

| 필드 | 건수 | 평균 길이 | 총 문자 | 현재 검색 |
|---|---:|---:|---:|---|
| 답사 노트 (`site_visits.notes`) | 96 | 559 | 53,646 | ❌ |
| 유지보수 조치 (`maintenances.resolution`) | 138 | 118 | 16,240 | ❌ |
| 유지보수 증상 (`maintenances.symptoms`) | 137 | 81 | 11,104 | ❌ |
| 티켓 코멘트 (`ticket_logs.content_html`) | 31 | 156 | 4,826 | ❌ |
| 유지보수 처리기록 (`maintenance_logs.content`) | 30 | 161 | 4,815 | ❌ |
| **합계** | **432** | | **90,631** | |

- 규모가 **작다**(9만 자). 그리고 **레코드 평균 81~559자로 짧다** → **청킹이 불필요하고, 레코드 자체가 곧 검색 단위**다
- 이 축은 앞으로 매일 늘어난다. 축 1과 반대로 **증분 갱신 설계**가 중요하다

### 1-3. 제품 버전 추적 부재 (미래 리스크)

- `device_info`에 등록된 기기는 **2종**(심전계 MC200M-T, 산소포화도 MP100W)이고 **버전 필드가 없다**
- **병원별로 어떤 thynC 버전이 설치됐는지 시스템이 모른다**
- 위키는 `thync_1.3.0`처럼 **버전별 카테고리** 구조다

→ 지금은 버전이 하나뿐이라 무해하다. 그러나 **1.4.0 산출물이 올라가는 순간부터 오답이 시작된다.** "알람 기준이 뭐야?"에 1.3.0과 1.4.0 문서가 함께 검색되어 섞인 답이 나가고, 병원마다 설치 버전이 다르면 **잘못된 매뉴얼을 안내**하게 된다. CS 응대에서 가장 치명적인 실패 유형이다.

**버전이 늘어나기 전에 넣는 것이 압도적으로 싸다.**

---

## 2. Part 1 — 축 1: 고정형 지식 정밀화

### W1. 위키 청크 인덱스 ★ (이 설계의 핵심)

문서를 **헤딩 단위로 쪼개 별도 인덱스**를 만든다. 검색 단위와 반환 단위를 문서에서 청크로 내린다.

**신규 테이블 `wiki.wiki_chunks`** (위키 스키마 — CLAUDE.md 규칙 8 준수)

| 컬럼 | 설명 |
|---|---|
| `id` | PK |
| `pageId` | → `wiki.wiki_pages` (Cascade) |
| `ordinal` | 문서 내 순서 |
| `headingPath` | `"3. 알람 정책 > 3.2 4단계 체계"` — 랭킹·표시 양쪽에 사용 |
| `text` | 청크 본문 (평문) |
| `charStart` / `charEnd` | 원문 위치 (전문 읽기 연계용) |
| `tokenEstimate` | 크기 관리용 |

- 인덱스: `(pageId, ordinal)`, `text` trigram GIN, `headingPath` trigram GIN
- **분할 규칙**: 목표 800~1,500자. h1~h4 경계 우선, 초과 시 문단 경계로 2차 분할. **표·코드블록은 절대 쪼개지 않는다**(구조 소실 방지 — 현재 "알람 4단계 표"가 문장으로 뭉개지는 문제의 직접 해법)
- **생성 시점**: 페이지 저장 시 `plainText` 재계산과 **같은 훅**에서 재생성. HTML은 `content_html` 파싱(`lib/wiki/htmlText.ts` 확장), BlockNote는 `content_json` 파싱(`lib/wiki/blockText.ts` 확장)
- 기존 109페이지는 1회 백필 스크립트

**효과**: API 규격서(68,772자)에서 필요한 200자를 찾을 때 — 현재 6,000자 × 최대 12회 → **관련 청크 2~3개(약 3,000자) 1회**

### W2. `search_wiki` 청크 단위 전환 + `read_wiki_chunk` 신설

- `search_wiki` 반환에 `chunkId`·`headingPath` 추가. 스니펫이 아니라 **청크 요약**을 반환하므로 모델이 읽을지 말지 정확히 판단할 수 있다
- 랭킹: **헤딩 경로 매칭 가중치**(제목보다 강한 신호) + 본문 토큰 AND 매칭 + trigram 유사도
- `read_wiki_chunk(chunkId, neighbors?)` 신설 — 해당 청크와 앞뒤 N개를 반환(문맥 확보)
- `read_wiki_page`는 유지(문서 전체 조망이 필요한 경우). `offset`/`nextOffset` 기존 동작 보존

### W3. 제품 버전 스코프 ★

1. **위키 쪽**: 버전 루트 카테고리에 메타 부여. AppSetting `wiki_product_version_roots`에 `{ "thync_1.3.0": <pageId>, ... }` 매핑. 신규 테이블 없이 처리
2. **운영 DB 쪽**: `hospital_meta`에 **`productVersion`** 컬럼 추가 (nullable — 미상 허용)
3. **도구 쪽**: `search_wiki`에 `version` 파라미터 추가. **병원 컨텍스트가 있으면 그 병원의 설치 버전 문서를 우선 검색**하고, 버전이 특정되지 않으면 최신 버전을 기본으로 하되 **답변에 어느 버전 기준인지 명시**하도록 시스템 프롬프트에 규정

> 3번의 "버전 명시"는 코드가 아니라 프롬프트 규칙이지만, CS 오안내를 막는 마지막 방어선이므로 설계 항목에 포함한다.

---

## 3. Part 2 — 축 2: 운영 지식 검색 신설

### O1. `search_operation_history` ★★ 가장 큰 공백

**현재 219건 유지보수의 조치 노하우와 96건 답사 노트가 어떤 경로로도 검색되지 않는다.** 이 축에서 가장 시급한 항목이다.

```
search_operation_history(query, workType?, hospitalCode?, from?, to?, limit?)
```

- **대상**: `maintenances.symptoms`/`resolution`, `maintenance_logs.content`, `site_visits.notes`, `etc_tasks.note`, `ticket_logs.content_html`(코멘트)
- **구현**: 총 9만 자 규모라 **벡터 없이 trigram GIN + 토큰 AND 매칭으로 충분**. 이미 `pg_trgm`이 설치돼 있고, 위키 검색에서 검증된 방식(리콜 4건→26건)을 그대로 적용
- **HTML 필드 처리**: Tiptap HTML을 매번 `regexp_replace`로 태그 제거하면 인덱스를 못 탄다 → **`plain_text` 파생 컬럼 + 트리거 또는 저장 훅**으로 미리 평문화 (위키 `plainText` 선례와 동일 패턴)
- **반환**: 유형·병원·날짜·발췌 + **원본 코드**(`MNT-YYYYMM-NNNN` 등 — 출처 링크용)

### O2. `find_similar_cases` — CS 응대 특화

```
find_similar_cases(symptoms, hospitalCode?, limit?)
```

증상 텍스트로 **과거 유사 장애와 그때의 조치**를 찾는다. O1의 특화 버전으로, 랭킹이 다르다:

- 증상 텍스트 유사도(trigram) + **같은 장애유형 가중** + 같은 병원 가중 + 최신 가중
- 반환에 **조치 요약(`resolution`)을 반드시 포함** — "어떻게 해결했는가"가 답의 본체

이 도구 하나가 CS 응대의 체감을 가장 크게 바꾼다고 본다. 나중에 티켓 접수 시 자동 분석(능동 기능)을 붙일 때도 그대로 재사용된다.

### O3. 상담이력 축적 루프 정상화

- **현재**: AI 정제 → 사람이 "병원 노트에 추가" 클릭 → 운영병원 193곳 중 **3곳**. 루프가 사실상 끊겨 있다
- **개선**: 세션 종료 시 초안 자동 생성 → 검토 후 1클릭 저장 (자동 저장은 하지 않는다 — 검토 없는 축적은 오염원이 된다)
- **축 분리 명시**: 병원 노트는 **저장 위치는 위키, 성격은 축 2**다. 따라서
  - 청킹(W1)·버전 스코프(W3) 규칙은 **적용하지 않는다**
  - `search_operation_history`(O1)의 **검색 대상에 포함**한다
  - 기존 `read_hospital_note`는 유지

> 프로젝트 이슈노트도 같은 성격이다(위키 저장 / 운영 정보). 동일하게 축 2로 분류한다.

---

## 4. Part 3 — 공통 기반 (두 축 공통)

### G1. 출처 표기 ★ 출시 전 필수

모든 지식 도구가 **원본 참조를 함께 반환**하도록 응답 규격을 통일한다.

| 축 | 출처 형태 |
|---|---|
| 축 1 | `pageId` + `headingPath` + 위키 링크 |
| 축 2 | 업무 코드(`MNT-…`/`VISIT-…`/`TK-…`) + 상세 링크 |

시스템 프롬프트에 "사실 진술에는 근거 출처를 함께 제시한다"를 규정한다. **검증할 수 없는 답은 업무에 쓰이지 않는다** — 출시 첫인상을 좌우하는 항목이다.

### G2. 피드백 수집 ★ 출시 전 필수

- 답변 하단에 👍/👎, 👎 선택 시 사유(**틀림 / 못 찾음 / 오래된 정보 / 부적절**)
- 신규 테이블 `ai_feedback`: `messageId`, `userId`, `verdict`, `reason`, `comment`, `createdAt`
- **`toolCalls`가 이미 메시지에 저장되므로, 어느 축·어느 도구에서 실패했는지 사후 분류가 가능하다**
- 이것이 §6(벡터DB 판단)의 근거 데이터가 된다. **오픈 전에 심어야 오픈 직후부터 쌓인다**

### G3. 데이터 격리 — 출시 차단 요소

- `/api/ai-assistant/chat`은 현재 `role === 'VIEWER'`만 검사하고 **소속(Organization) 검사가 없다**
- nav 메뉴의 `allowed_org_codes`는 비어 있고, 그마저 **UI 게이트일 뿐 API는 그대로 열린다**
- 현재 노출은 없다(대웅제약 활성계정 1명이 VIEWER). 그러나 대웅 소속 USER 계정이 생기면 12개 도구로 전사 데이터를 열람한다
- **결정 필요(§7-1)**: 자사 전용이면 서버 측 소속 검사 한 줄. 대웅에도 열 거면 **도구 레이어에 소속별 필터**가 필요하다(예: 담당 배정된 병원만) — 이 경우 작업량이 상당히 커진다

### G4. 컨텍스트 관리

- 히스토리 30메시지 + 8회 루프 도구 결과가 그대로 누적된다. 비용은 캐시로 눌렀으나 **컨텍스트 오염**은 남아 있다
- context editing(`clear_tool_uses`)으로 오래된 tool_result를 정리. 긴 세션의 판단 품질 문제이며, 비용 절감이 목적이 아니다

---

## 5. 작업 구성

| Phase | 항목 | 규모 | 비고 |
|---|---|---|---|
| **V1** | G3 데이터 격리 · G1 출처 표기 · G2 피드백 수집 | S | 출시 전 필수. 서로 독립이라 병렬 가능 |
| **V2** | O1 운영 텍스트 검색 · O2 유사 사례 | M | 축 2의 공백을 메움. 파생 평문 컬럼 + 인덱스 포함 |
| **V3** | W1 청크 인덱스 · W2 청크 검색 | **L** | 이 설계에서 가장 무겁고 효과도 가장 큼 |
| **V4** | W3 버전 스코프 · O3 축적 루프 · G4 컨텍스트 관리 | M | V3 이후 |

**권장 순서: V1 → V2 → V3 → V4.**

V2를 V3보다 앞에 두는 이유는 셋이다. ① 축 2는 현재 **검색이 0**이라 개선 폭이 가장 크다 ② 규모가 작아(9만 자) 벡터 없이 확실히 해결된다 ③ V3(청킹)은 위키 문서가 늘어나기 전에만 하면 되므로 상대적으로 덜 급하다.

### 검증

- 두 축을 나눈 **회귀 질문셋 20문항**(고정형 10 / 운영 10)을 착수 전 함께 확정
- `scripts/ai-agent-smoke.mts`(기존)를 확장해 Phase 전후 비교 — 정답성·도구 호출 수·토큰
- 판정: **정답성 회귀 0건**이면서 도구 호출 수 감소(= 한 번에 답을 찾음)

---

## 6. 벡터DB(RAG) 판단 — 지금은 하지 않는다, 단 기준을 정해둔다

**결론: 현 규모에서는 과잉이고, "정보를 다 위키화"한 뒤에는 필요해진다. 다만 청킹(W1)이 반드시 먼저다.**

### 근거

1. **규모가 작다.** 축 1이 21.7만 자, 축 2가 9만 자. 이 규모에서 trigram + 토큰 매칭은 잘 동작한다(위키 검색 개선에서 리콜 4→26건 확인)
2. **키워드와 벡터는 이기는 영역이 다르다.** 순수 벡터 전환은 오히려 퇴보한다

| 질의 유형 | 키워드 | 벡터 |
|---|---|---|
| 모델명·고유명사 (MC200M-T, 게이트웨이) | **강함** | 약함 |
| 병원명·업무 코드 | **강함** | 약함 |
| 개념 질의 ("자꾸 끊겨요") | 약함 | **강함** |
| 동의어 (장애/이슈/문제) | 약함 | **강함** |

CS 응대는 양쪽이 다 필요하므로, 도입 시에도 **하이브리드(키워드 + 벡터를 각각 뽑아 RRF 병합)** 가 정답이다.

3. **외부 벤더 의존이 늘어난다.** Anthropic은 임베딩 API를 제공하지 않는다. pgvector를 쓰려면 Voyage AI·OpenAI 등 **별도 벤더 계약**이 필요하다. 현재 Anthropic 단일 의존인 구조가 깨진다 — 기술 판단이 아니라 경영 판단 영역이다
4. **청킹이 선행되어야 한다.** 청크가 곧 임베딩 단위다. W1을 해두면 나중에 벡터는 **인덱스만 얹으면** 된다. 반대로 청킹 없이 문서 통짜를 임베딩하면 효과가 잘 나지 않는다

### 도입 판단 기준 (아래 중 **둘 이상** 충족 시 재검토)

- 위키 페이지 **300개 초과** 또는 청크 **3,000개 초과**
- 축 2 운영 텍스트 **50만 자 초과** (현재 9만 자)
- G2 피드백에서 **"못 찾음" 비율 15% 초과**가 2개월 연속
- 헤딩 경로 랭킹으로 해결되지 않는 개념 질의 실패가 반복 확인

즉 **"벡터DB는 V5 후보"** 이며, 판단 근거는 G2가 만들어 준다.

---

## 7. 결정 필요 항목 (착수 전)

| # | 항목 | 선택지 | 영향 |
|---|---|---|---|
| **1** | **어시스턴트 공개 범위** | (a) 자사(SEERS) 전용 (b) 대웅제약 포함 | (b)면 도구 레이어 전체에 소속별 필터가 필요해 **V1 규모가 S→L로 커진다** |
| **2** | 제품 버전 관리 | (a) 1.4.0 계획 있음 → W3를 V2로 앞당김 (b) 당분간 1.3.0 단독 → V4 유지 | 버전이 둘 되는 순간 오답이 시작되므로 시점이 중요 |
| **3** | 병원별 설치 버전 입력 | (a) `hospital_meta.productVersion` 수기 입력 (b) 프로젝트 완료 시 자동 기록 (c) 도입 안 함 | (c)면 W3는 "최신 버전 우선 + 버전 명시"까지만 가능 |
| **4** | 상담이력 저장 방식 (O3) | (a) 초안 생성 후 사람이 승인 (권장) (b) 자동 저장 | (b)는 축적은 빠르나 검토되지 않은 내용이 지식원을 오염시킨다 |
| **5** | 착수 범위 | V1만 / V1+V2 / 전체 | — |

> 1번이 다른 모든 항목의 규모를 좌우하므로 **가장 먼저 정해주시길 권합니다.**

---

## 8. 이 설계가 바꾸지 않는 것

- **에이전트 루프 구조** — 스트리밍 + tool use 반복 + read-only 화이트리스트는 정석이며 유지한다. 문제는 루프가 아니라 루프가 접근하는 **지식의 구조**였다
- **쓰기 권한** — 조회 전용을 유지한다. 능동 기능(티켓 자동 분석·Slack·브리핑)은 출시 후 G2 피드백을 보고 별도 판단한다
- **위키 카테고리 제외(`ai_excluded`)** — 이미 요구사항을 충족하므로 손대지 않는다
- **모델·비용 설정** — 직전 배포분(opus-5 / effort medium / 롤링 캐시) 유지

---

## 9. 결정 결과 (2026-07-25)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 공개 범위 | **자사(SEERS) 전용** → V1은 S 규모로 확정 |
| 2·3 | 제품 버전 관리 / 병원별 설치 버전 | **보류** — 1.4.0 릴리스 계획이 필요한 사업 판단이라 W3 미착수 (§11) |
| 4 | 상담이력 저장 | (a) 초안 후 승인 — O3는 V4로 이월 |
| 5 | 착수 범위 | V1 + V2 + V3 (W3 제외) |

---

## 10. 구현 결과 (DEV)

### 10-1. 축 1 — 위키 청킹

**109페이지 → 431청크** (평균 4.0), 최대 청크 1,954자. 상한 초과 0건.

| 문서 | 원본 | 청크 수 | 최대 청크 |
|---|---:|---:|---:|
| 05. API 규격서 | 68,772자 | **97** | 1,954자 |
| 06. 데이터베이스 설계서 | 35,141자 | 42 | 1,885자 |
| 03. 기능정의서 — 서비스 콘솔 | 21,250자 | 41 | 1,389자 |

- 헤딩 경로가 검색 결과에 함께 온다: `05. API 규격서 > 1. 공통 규격 > 1.2 인증 (TokenInterceptor)`
- **표 구조 보존 확인** — `표시명 | 타입코드 | 설명 | 기본 레벨` 한 줄로 유지되어, 기존 평문 추출에서 표가 문장으로 뭉개지던 문제가 해소됨
- 문서 제목과 중복되는 h1은 경로에서 제거(토큰 낭비·랭킹 노이즈 방지)

### 10-2. 축 2 — 운영 정보 검색

기존에 **검색 수단이 없던** 432건 / 90,631자가 검색 가능해졌다.

- `search_operation_history("게이트웨이 오프라인")` → 관련 **118건** 중 상위 N건, 전부 실제 게이트웨이 통신 이슈
- `find_similar_cases("심전계가 연결이 끊어지고 데이터가 안 올라옵니다")` → 유사 장애 3건 + **각각의 조치 내용**
- 짧은 레코드(평균 81~559자)에는 토큰 AND 매칭이 리콜을 0으로 만들어, **1개 이상 매칭을 통과 조건으로 두고 매칭 수·제목 가중·유사도로 랭킹**하는 방식으로 조정

### 10-3. 회귀 질문셋 7건 (2축) — 전부 정답

`npx tsx scripts/ai-agent-smoke.mts`

| 축 | 질문 | 도구 | 캐시적중 | 비용 |
|---|---|---:|---:|---:|
| 1 | 알람 4단계 체계 | 1 | 61.3% | $0.1040 |
| 1 | API 인증 방식 | 1 | 85.8% | $0.0404 |
| 1 | 부정맥 알람 분류 | 2 | 82.7% | $0.0847 |
| 2 | 게이트웨이 장애 사례·조치 | 2 | 74.4% | $0.0839 |
| 2 | 심전계 끊김 유사 사례 | 1 | 92.0% | $0.0373 |
| 2 | 이번달 유지보수 건수 | 1 | 96.1% | $0.0197 |
| 교차 | 병원 현황 + 장애 이력 | 5 | 94.8% | $0.0565 |

**전체 캐시 적중률 82.8% · 건당 평균 $0.0609.** 첫 질문의 적중률이 낮은 것은 도구 정의가 바뀌어 캐시가 1회 무효화된 정상 동작이다.

**출처 표기 작동 확인**: `[MNT-202605-0043](/maintenances/104)`, `[08. 알람·부정맥 임상 정책서](/wiki/bd59…)` 형태의 마크다운 링크가 답변에 포함됨.

### 10-4. 도구 구성 변화

12종 → **15종**. 신설 `search_operation_history` · `find_similar_cases` · `read_wiki_chunk`.
`search_wiki`는 문서 단위 → **절 단위**로 전환(반환 필드 변경), `read_wiki_page`는 문서 조망용으로 존치.

---

## 11. 보류·미채택 항목

### W3 제품 버전 스코프 — **보류**

§7-2·3 결정이 필요하다. 1.4.0 산출물이 언제 올라가는지, 병원별 설치 버전을 어떻게 채울지(수기/자동/미도입)가 정해져야 설계가 확정된다. **버전이 둘이 되기 전에는 오답이 발생하지 않으므로 지금 급하지 않지만, 둘이 되는 시점보다는 반드시 먼저 들어가야 한다.**

### G4 컨텍스트 관리(context editing) — **미채택, 설계 판단 수정**

설계 §4-G4의 전제가 실측과 달랐다.

- 도구 결과는 **턴을 넘어 누적되지 않는다.** `chat/route.ts`는 답변의 최종 텍스트만 DB에 저장하고 히스토리를 그것으로 재구성하므로, tool_result는 해당 턴 안에서만 존재한다(최대 8회 반복분)
- 즉 "긴 세션에서 초반 도구 결과가 판단을 흐린다"는 우려는 실제 구조에서 발생하지 않는다
- 반면 context editing은 프롬프트 앞부분 바이트를 바꾸므로 **직전 배포에서 확보한 롤링 캐시(적중률 82~96%)를 무효화한다**

이득이 없고 손해가 확실하므로 적용하지 않는다. 히스토리 30메시지 누적은 캐시가 0.1배로 흡수하고 있다.

### O3 상담이력 축적 루프 — **V4로 이월**

병원 노트 3/193 문제는 그대로다. 다만 이번 범위(지식 아키텍처)와 성격이 다르고 UI 작업이 함께 필요해 분리한다.

---

## 12. PROD 반영 체크리스트

1. `git push` → PROD `git pull`
2. **마이그레이션 2건**: `npx prisma migrate deploy` (`20260725120000_add_ai_feedback`, `20260725120100_add_wiki_chunks`) → `npx prisma generate`
3. **nav 시드 재실행**: `psql -U thync -d thync_ops -f scripts/seed-ai-assistant-nav.sql` — 어시스턴트 메뉴 3종을 `{SEERS}`로 스코핑하는 UPDATE가 추가됨
4. **청크 백필**: `npx tsx scripts/backfill-wiki-chunks.mts` — **이걸 빠뜨리면 위키 검색이 0건이 된다**(청크 테이블이 비어 있으므로)
5. 힙 4GB 빌드 → `pm2 restart thync-prod`
6. 검증: `/settings/ai-assistant` 접근, 위키 질문 1건(축1)·장애 사례 질문 1건(축2), `wiki_chunks` 행 수 확인

> 신규 패키지 없음. 대웅제약 계정은 이 배포 이후 어시스턴트 API가 403이 된다(현재 활성 1명이 VIEWER라 실사용 영향 없음).
